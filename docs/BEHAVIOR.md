# Поведение FinanceTracker

Этот документ описывает фактическое поведение `tracker` и `bot` по текущему коду, включая источники данных, формулы и fallback-ветки.

## Доступ И Доставка Сообщений

- Бот отвечает только пользователям из `ALLOWED_USER_IDS`.
- Этот же набор используется как `TARGET_CHAT_IDS` для автоматических сообщений.
- Неавторизованные пользователи игнорируются молча.
- Отправка текста идёт через `safe_send_message(...)`: если Markdown ломается на динамических данных, бот повторяет ту же отправку без `parse_mode`.

## Как Проект Читает Данные

### Dedup + только `OPERATION_STATE_EXECUTED`

Финансовые запросы к `operations` используют общий `OPERATIONS_DEDUP_CTE`: последняя строка выбирается по ключу `account_id + COALESCE(operation_id, id)` и затем фильтруется по `state = OPERATION_STATE_EXECUTED`.

Этот слой применяется в расчётах `/today`, `/week`, `/month`, `/year`, `/history`, `/twr`, dataset и trigger выполнения годового плана. Отменённые/исполняющиеся операции и повторные строки одного `operation_id` не входят в суммы.

Исполненное пополнение может иметь ручную категорию `cashflow_category=iis_tax_deduction`. Такое поступление считается доходом портфеля: оно не входит в собственные пополнения, годовой план и внешний денежный поток TWR/XIRR, но входит в дельту и совокупный результат. Синхронизация операций не перезаписывает ручную категорию.

### Зависимость от `income_events`

`income_events` используется для:

- купонов и дивидендов в `/today`, `/week`, `/month`;
- части налогов в `/today`, `/week`, `/month`;
- минутных уведомлений о зачислении дохода.

Если таблицы `income_events` нет:

- купоны и дивиденды для этих отчётов становятся `0`;
- налоги считаются только по налоговым типам операций из `operations`;
- минутные уведомления о купонах и дивидендах не работают.

## Команды Бота

### `/start`

- Доступна только авторизованным пользователям.
- Возвращает короткое приветствие и отправляет к `/help`.

### `/help`

- Показывает список команд и фоновых задач.
- Показывает время ежедневного JobQueue вместе с таймзоной `TIMEZONE`.

### `/today`

Источник данных:

- последние 2 записи из `portfolio_snapshots`;
- сумма собственных пополнений из `operations` по типу `OPERATION_TYPE_INPUT` без категории вычета;
- внешние пополнения и выводы из `operations` за локальный день `TIMEZONE` для корректировки дневной дельты;
- купоны и дивиденды из `income_events`, а также налоговый вычет ИИС из `operations` за локальный день `TIMEZONE`;
- комиссии и налоги из `operations` и `income_events`.

Как считаются ключевые поля:

- `snapshot_dt` это `snapshot_at`, конвертированный в `TIMEZONE`;
- дельта за день = `последний total_value - предыдущий total_value - net_external_flow_day`, где `net_external_flow_day = deposits - withdrawals`;
- общий PnL = `последний total_value - сумма собственных пополнений`.

Поведение:

- если снапшотов нет, возвращается `Пока нет ни одного снапшота портфеля.`;
- если снапшот один, дневная дельта не считается;
- выводы исключаются из дневной дельты, но отдельной строкой не показываются;
- текст выбирается случайно из набора шаблонов в [`src/bot/today_templates.py`](../src/bot/today_templates.py), поэтому формулировка может меняться при одинаковых числах.

### `/week`

Период:

- текущая рабочая неделя понедельник-пятница, определённая в `TIMEZONE`.

Источник данных:

- последний снапшот как текущая стоимость;
- последний снапшот строго до начала недели как база для сравнения;
- собственные пополнения, выводы, купоны, дивиденды, вычеты ИИС, комиссии и налоги за период текущей рабочей недели;
- пополнения с начала года для прогресса плана.

Как считаются ключевые поля:

- недельная дельта = `current_value - last_snapshot_before_monday - net_external_flow_week`, где `net_external_flow_week = deposits - withdrawals`;
- недельный процент = `delta / base`;
- прогресс плана = `пополнения с 1 января / PLAN_ANNUAL_CONTRIB_RUB`.

Fallback:

- если снапшота до начала недели нет, бот считает, что портфель появился внутри недели: `week_delta_abs = current_value - net_external_flow_week`, `week_delta_pct = 0.0`;
- если снапшотов нет вообще, возвращается `Пока нет ни одного снапшота портфеля.`;
- выводы исключаются из недельной дельты, но отдельной строкой не показываются;
- текст выбирается случайно из [`src/bot/week_templates.py`](../src/bot/week_templates.py).

### `/month`

Период:

- текущий календарный месяц в `TIMEZONE`.

Источник данных:

- собственные пополнения, выводы, купоны, дивиденды, вычеты ИИС, комиссии и налоги за текущий месяц;
- пополнения YTD для годового плана;
- последний снапшот строго до начала месяца и последний снапшот внутри месяца;
- позиции этих двух снапшотов для блока изменений.

Как считаются ключевые поля:

- изменение за месяц = `end_snap.total_value - start_snap.total_value - net_external_flow_month`, где `net_external_flow_month = deposits - withdrawals`;
- прогресс годового плана = `dep_year / plan`;
- фраза о статусе графика выбирается случайно по сравнению `dep_year` с линейным ориентиром на текущую дату:
  - `>= 105%` ориентира: впереди графика;
  - `>= 95%` ориентира: около графика;
  - иначе: позади графика.

Блок изменений позиций:

- строится по `figi` между начальным и конечным снапшотом месяца;
- категории: новая позиция, закрыта, увеличена, уменьшена;
- сортировка внутри категории идёт по тикеру.

Fallback:

- если нет стартового снапшота до начала месяца, изменение не считается;
- если нет конечного снапшота в месяце, текущая стоимость остаётся `0`;
- выводы исключаются из месячной дельты, но отдельной строкой не показываются;
- текст выбирается случайно из [`src/bot/month_templates.py`](../src/bot/month_templates.py).

### `/year`

Аргументы:

- без аргумента: текущий год YTD;
- `/year YYYY`: указанный календарный год.

Источник данных:

- снапшоты начала и конца периода;
- dedup-слой `operations_dedup`;
- только операции со `state = OPERATION_STATE_EXECUTED`;
- позиции первого и последнего снапшота внутри периода;
- `income_events` здесь не используются, расчёты идут прямо по `operations`.

Что отправляет команда:

1. summary-текст;
2. PNG-график `стоимость портфеля и пополнения`, если данных достаточно;
3. PNG-график `прирост/падение по месяцам`, если данных достаточно;
4. текстовый блок изменений позиций.

То есть при нормальных данных `/year` обычно отправляет 4 сообщения, но при недостатке данных графиков может быть меньше.

Как считаются ключевые блоки summary:

- стоимость на конец периода = последний снапшот внутри периода;
- изменение стоимости = `end_snap.total_value - start_snap.total_value`, где `start_snap` это последний снапшот строго до начала периода;
- прогресс плана = `YTD deposits / PLAN_ANNUAL_CONTRIB_RUB`;
- `Реализовано` по активам = сумма `yield + commission` по продажам `OPERATION_TYPE_SELL`;
- `Дивиденды/купоны` по активам = сумма дивидендов, купонов и соответствующих налоговых строк;
- `По открытым позициям` = сумма `expected_yield` последнего снапшота на конец периода.

Как строится блок изменений позиций:

- берутся первый и последний снапшот внутри периода;
- позиции агрегируются по "логическому активу" в порядке `ticker -> name -> figi`;
- валютные позиции, включая `RUB000UTSTOM` и `instrument_type=currency`, исключаются;
- категории: `🆕 Новые`, `📈 Докупили`, `📉 Продали часть`, `✅ Закрыли`;
- блок `🆕 Новые` показывается только если период начинается 1 января.

Fallback:

- если внутри периода меньше двух снапшотов, вместо diff-блока возвращается сообщение о нехватке данных;
- если нет данных для первого графика, бот отправляет текст `Недостаточно данных для графика ...`;
- если нет данных для второго графика, второй PNG просто не отправляется.

### `/structure`

- Берёт последний снапшот и его позиции.
- Группирует позиции по типам инструментов: акции, облигации, ETF, валюта, фьючерсы, другое.
- Для каждой группы показывает сумму, долю портфеля и совокупный P&L.
- Затем перечисляет бумаги внутри группы по убыванию стоимости позиции.

Fallback:

- если снапшотов нет, сообщение `Нет ни одного снапшота портфеля.`;
- если в снапшоте нет позиций, сообщение `В последнем снапшоте нет позиций.`

### `/history`

- Строит PNG-график стоимости портфеля, кумулятивных собственных пополнений и отдельной серии вычетов ИИС.
- Берёт `portfolio_snapshots` как временной ряд и `operations` как источник пополнений.
- Ось X строится недельным шагом от первой до последней даты снапшота.
- Для каждой точки графика берётся последний снапшот `<= date` и сумма всех пополнений `<= date`.
- Максимум нарисован отдельной красной точкой с подписью даты и значения на графике.

Fallback:

- если снапшотов меньше двух, бот отвечает `Недостаточно данных для построения графика.`

### `/twr`

- Агрегирует `portfolio_snapshots` по `snapshot_date`.
- Читает собственные пополнения и выводы из `operations`, исключая категорию `iis_tax_deduction`, и переводит naive UTC timestamp в локальную дату `TIMEZONE`.
- Считает TWR посуточно по формуле `(V - CF) / V_prev - 1`.
- XIRR также не считает вычет внесённым капиталом: сумма вычета остаётся в конечной стоимости и увеличивает доходность.
- Отправляет короткий текст с текущим TWR и стоимостью портфеля, затем PNG-график.

Fallback:

- если исторических точек меньше двух, бот отвечает `Недостаточно данных`.
- если `V_prev` отсутствует, нулевой или текущая точка `None`, формула для этой точки не пересчитывается, а серия продолжает последнее известное значение.

### `/calendar`

- Показывает купоны и объявленные дивиденды за 90 календарных дат: с сегодняшней локальной даты до `today + 89 дней` включительно.
- Купоны берутся из `InstrumentsService/GetBondCoupons`, дивиденды — из `InstrumentsService/GetDividends`.
- Для дивиденда используется `payment_date`, для купона — `coupon_date`.
- Ожидаемая сумма до налога равна выплате на одну бумагу, умноженной на количество инструмента в последнем снапшоте. В ответе из неё вычитается расчётный налог `PAYOUT_CALENDAR_TAX_RATE_PCT` (по умолчанию 13%).
- Известные и оценочные суммы агрегируются по календарным месяцам; валюты не складываются, а события без суммы считаются отдельно в каждом месяце.
- Если будущий купон ещё не опубликован, бот берёт последнюю известную выплату на одну облигацию того же выпуска, умножает её на текущее количество бумаг и помечает сумму знаком `~`. Такой купон входит в общий и помесячный итоги, которые также помечаются `~`.
- Для купона с валидными границами периода и положительным текущим cost basis
  рядом с суммой показывается простой годовой эквивалент:
  `net_expected_amount / cost_basis × 100 × 365 / period_days`.
- Годовой эквивалент использует `ACT/365F`, считается после расчётного налога,
  не предполагает реинвестирование и не является YTM или прогнозом доходности.
- Если нет ни опубликованной суммы, ни предыдущего известного купона, выплата помечается как `сумма уточняется`.
- Отменённые дивиденды не показываются.
- RUB, USD и другие валюты агрегируются отдельно.
- Ответ ограничен первыми 30 событиями; количество остальных событий показывается отдельной строкой.
- Это оценка по текущим позициям: продажа бумаги, изменение выплаты и фактический налог могут изменить результат.

Календарь хранится в `payout_calendar_events`. `tracker` обновляет его при старте и ежедневно в `PAYOUT_CALENDAR_SYNC_HOUR:PAYOUT_CALENDAR_SYNC_MINUTE` по `SCHED_TZ`. Ошибка одного инструмента не удаляет его ранее сохранённые события; успешный пустой ответ удаляет отменённые или исчезнувшие события в активном окне.

### Меню Команд Telegram

- При каждом успешном старте `bot` регистрирует актуальный список команд через
  Telegram Bot API `setMyCommands`.
- Меню и обработчики строятся из одного `COMMAND_SPECS`, поэтому новая команда не
  должна работать «скрыто», отсутствуя в подсказках Telegram.

## Автоматические Сообщения

### Ежедневный JobQueue

`daily_job(...)` выполняется каждый день в `DAILY_SUMMARY_HOUR:DAILY_SUMMARY_MINUTE` по таймзоне `TIMEZONE` (по умолчанию `18:00 Europe/Moscow`).

Если бот стартовал позже этого времени, одноразовый startup catch-up добирает пропущенный daily job за текущую дату.

Он может отправить:

- текстовый месячный отчёт и monthly PDF, если сегодня последний день месяца;
- недельный отчёт, если сегодня пятница;
- trigger-сообщение о выполнении годового плана.

Если на конкретную дату отправлять нечего, джоб завершается тихо.
Повторный запуск за ту же дату подавляется через таблицу `bot_daily_job_runs`.

### Понедельничная сводка выплат

`weekly_payout_digest(...)` выполняется каждый понедельник в `PAYOUT_WEEKLY_HOUR:PAYOUT_WEEKLY_MINUTE` по `PAYOUT_WEEKLY_TIMEZONE` (по умолчанию `10:00 Europe/Moscow`).

- Период сводки: понедельник–воскресенье включительно.
- Отправляется и пустая сводка: пользователь явно видит, что ожидаемых выплат на неделе нет.
- Поздний рестарт в понедельник после времени отправки запускает startup catch-up.
- Повторная отправка в ту же дату подавляется через `bot_daily_job_runs` с `job_name=weekly_payout_digest`.
- Если все отправки завершились ошибкой, claim освобождается для повторного catch-up в тот же день.

Month-end delivery policy:

- auto-send в последний день месяца отправляет текстовый `/month` и PDF-версию через внутренний `reporter`;
- если `reporter` недоступен или PDF не собрался, текстовый отчёт всё равно отправляется;
- если PDF не доставлен ни в один целевой чат, claim `monthly_pdf_delivery` освобождается для повторного добора в тот же день;
- monthly PDF auto-send использует отдельный `job_name=monthly_pdf_delivery`, поэтому может ретраиться независимо от `daily_summary`;
- startup catch-up может добрать `monthly_pdf_delivery`, даже если `daily_summary` за ту же дату уже был закрыт ранее.

### Утренний Триггер Нового Максимума

`yesterday_peak_alert(...)` выполняется каждый день в `08:00` по `TIMEZONE`.

Если бот стартовал позже этого времени, одноразовый startup catch-up добирает проверку за вчерашнюю дату.
Повторный запуск за ту же дату подавляется через таблицу `bot_daily_job_runs` с `job_name=yesterday_peak_alert`.

Условие:

- итоговая стоимость снапшота за вчера строго больше максимума всех `total_value` до вчерашней даты.

Сообщение содержит:

- дату и итоговую стоимость вчерашнего снапшота;
- сумму предыдущего максимума;
- дату предыдущего максимума.

Особенность:

- для самого первого дня trigger не отправляется, потому что исторического максимума "до этого дня" ещё нет;
- дневной снапшот может обновляться в течение дня, поэтому максимум проверяется утром по завершённому вчерашнему дню, а не в вечернем `daily_job`.

### Триггер Выполнения Годового Плана

Условие:

- сумма пополнений с 1 января до вчера была меньше плана;
- сумма пополнений с 1 января до сегодня стала больше или равна плану.

Источник данных:

- dedup-слой `operations_dedup` по типу `OPERATION_TYPE_INPUT`;
- только `state = OPERATION_STATE_EXECUTED`.

### Уведомления О Купонах И Дивидендах

`check_income_events(...)` запускается каждые 60 секунд, первый раз через 10 секунд после старта.

Логика:

1. Читает все `income_events` с `notified = false`.
2. Для каждой строки отправляет отдельное сообщение во все `TARGET_CHAT_IDS`.
3. Помечает событие как `notified = true` только если отправка успешно прошла во все чаты.

Формат:

- купон: `💸`
- дивиденды: `💰`
- в тексте есть имя инструмента, тип события, сумма и доходность к cost basis.
- Для купона с однозначно сопоставленными границами купонного периода дополнительно
  показывается простой годовой эквивалент: `net_yield_pct × 365 / period_days`.
- Годовой эквивалент использует `ACT/365F`, не предполагает реинвестирование и не
  является YTM, прогнозом следующего купона или полной доходностью владения.
- Если период отсутствует, некорректен или неоднозначен, дополнительная строка не
  показывается. Для дивидендов годовой эквивалент не рассчитывается.

## Поведение `tracker`

### Снапшоты

- В каждый локальный день `SCHED_TZ` хранится один снапшот на счёт.
- Повторный запуск в этот же день обновляет агрегаты и полностью пересоздаёт позиции снапшота.
- Если для позиции в БД нет `ticker` или `name`, `tracker` дёргает `GetInstrumentBy` и дозаполняет `instruments`.

### Операции

- Синхронизация использует `GetOperationsByCursor` с `withoutTrades=true`.
- Запрос постраничный, курсорный; граница `to` фиксируется один раз на весь проход.
- `OPERATIONS_MAX_PAGES` защищает от бесконечной пагинации.
- Повторный cursor, `hasNext=true` без `nextCursor`, некорректный ответ или достижение лимита до конца выдачи завершают синк ошибкой. Частично загруженные операции откатываются, а не фиксируются как успешный инкрементальный синк.
- Все read-only запросы T-Invest используют одну keep-alive HTTP-сессию. Transport-ошибки, `408`, `429` и `5xx` повторяются с ограниченным backoff; `Retry-After` и `x-ratelimit-reset` имеют приоритет.
- Динамические портфель и операции не кешируются. Успешные метаданные `GetInstrumentBy` хранятся в ограниченном process-local TTL/LRU-кеше.

### Backfill Полей `OperationItem`

Если в таблице `operations` у существующих строк для счёта есть `state IS NULL`, `tracker` считает схему неполной и запускает backfill от даты открытия счёта, а не от последней известной операции.

### Формирование `income_events`

- `OPERATION_TYPE_COUPON` и `OPERATION_TYPE_DIVIDEND` считаются gross.
- Купонными налогами считаются актуальные типы T-Invest `OPERATION_TYPE_BOND_TAX`, `OPERATION_TYPE_BOND_TAX_PROGRESSIVE` и legacy-тип `OPERATION_TYPE_COUPON_TAX`.
- Дивидендными налогами считаются `OPERATION_TYPE_DIVIDEND_TAX` и `OPERATION_TYPE_DIVIDEND_TAX_PROGRESSIVE`.
- В агрегацию входят только `OPERATION_STATE_EXECUTED`.
- `net_amount = gross + tax`.
- При создании события или коррекции его денежных сумм `net_yield_pct = net_amount / latest_cost_basis * 100`, если cost basis положительный; неизменённые исторические события не пересчитываются из-за последующего движения cost basis.
- Последний cost basis выбирается в пределах того же `account_id`, чтобы позиция
  с тем же FIGI на другом счёте не влияла на расчёт.
- После каждого успешного синка затронутые доходные события пересчитываются по полной локальной истории ключа, поэтому поздний налог обновляет существующую строку, даже если исходная выплата не входила в текущее API-окно.
- Уже отправленное уведомление при коррекции суммы повторно не отправляется.
- Отсутствие ключа среди локальных исполненных операций не удаляет исторический `income_event`: неполная локальная история не считается доказательством отмены события.

## Известные Нюансы

- T-Invest допускает изменение `operation_id` со временем; текущий dedup не связывает такие версии через `parent_operation_id`, поэтому логические дубли остаются отдельным риском качества данных.
- `TIMEZONE` задаёт civil time ежедневного и утреннего JobQueue через timezone-aware `datetime.time`.
- На чистой БД без SQL-миграции `deposits` остаётся таблицей, хотя новый код уже работает через `operations`.
- Формулировки `/today`, `/week` и `/month` недетерминированы из-за случайного выбора шаблона; числовая часть при этом берётся из одних и тех же расчётов.
